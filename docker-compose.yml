version: '3.7'

services:
  postgres:
    image: bitnami/postgresql:latest
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: taskment
    ports:
      - "6060:5432"
    volumes:
      - postgres_data:/bitnami/postgresql

volumes:
  postgres_data: